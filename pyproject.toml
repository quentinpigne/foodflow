[project]
name = "foodflow"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["frontend", "backend"]

[tool.uv.sources]
frontend = { workspace = true }
backend = { workspace = true }

[tool.uv.workspace]
members = ["packages/frontend", "packages/backend"]
